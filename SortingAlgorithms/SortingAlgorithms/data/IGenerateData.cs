namespace SortingAlgorithms.data
{
    public interface IGenerateData
    {
        int[] getTable(int size);    // this method should be implemented in every class that 'extends' this interface
                                     // use your own signature of this method if needed
    }
}